"""Proactive Agent Ecology — runtime support modules.

Holds Substrate code (persistence, helpers) that workflow `transform_flow`
nodes import. The conceptual blueprint lives at docs/proactive.md;
the in-repo changelog is at examples/proactive-CHANGELOG.md.
"""
